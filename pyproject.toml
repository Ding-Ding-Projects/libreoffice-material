[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "libreoffice-material"
version = "0.0.1"
description = "Material Design theme for LibreOffice"
requires-python = ">=3.10"

[tool.setuptools]
packages = []
